# PT-Gen Environment Variables

# Author name displayed in API responses
AUTHOR=Rhilip

# API key for access control (optional)
# If set, requests must include &apikey=YOUR_API_KEY
# APIKEY=your-secret-key

# Disable search functionality (optional)
# Set to any non-empty value to disable search
# DISABLE_SEARCH=true

# Douban cookie for accessing login-required content (optional)
# DOUBAN_COOKIE=bid=xxx; ll=xxx

# Indienova cookie for accessing restricted content (optional)
# INDIENOVA_COOKIE=session=xxx

# =============================================================================
# PROXY CONFIGURATION (Docker deployments only)
# =============================================================================
# Used for bypassing geo-restrictions (e.g., accessing Douban from outside China)
# Format: http://username:password@hostname:port
# 
# Examples:
#   HTTP_PROXY=http://user:pass@proxy.example.com:8080
#   HTTP_PROXY=http://3MIYW8gRPmd1skwx:ssDy64ogyTwjjuys_country-cn@geo.iproyal.com:12321
#
# Note: Cloudflare Workers edge runtime does NOT support HTTP proxies.
#       This only works when running in Docker/Node.js environment.

# HTTP_PROXY=
# HTTPS_PROXY=
# Or use PROXY_URL as an alternative (either works)
# PROXY_URL=
